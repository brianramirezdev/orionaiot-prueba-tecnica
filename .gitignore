# Dependencias y builds (cada subproyecto tiene su propio .gitignore también)
node_modules
dist
dist-ssr

# Herramientas locales de desarrollo, no forman parte del proyecto
.claude
.remember

# Enunciado de la prueba: se queda en local, no se sube al repo
/PRUEBA DE ReactJs PROGRAMADOR WEB.pdf

*.log
.DS_Store
